Which Git command takes changes made on the local machine and transfers the changes to a remote repository?

- git push

What is the difference between Git and GitLab?

- git is an open source version contrl system and gitlab is a host for git repos

What does the command git branch (without any options) do?

- list all branches

What does “merge conflict” mean in version control?

- different changes to the same code while merging branches
